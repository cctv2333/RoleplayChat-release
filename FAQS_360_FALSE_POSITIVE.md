# ❓ FAQ：360 报毒怎么办？(HEUR/QVM202.0.72EF.Malware.Gen)

> **一句话结论：这是误报。** 我们的软件是开源项目，代码完整公开在 GitHub，不存在病毒。
> 如果你看到 360 拦截本软件的安装包，请先不要惊慌，读完下面 3 分钟就能解决。

---

## 1. 为什么会报毒？

360 报的名称是 `HEUR/QVM202.0.72EF.Malware.Gen`：

- `HEUR` = **启发式检测**（靠"看起来像"判断，不是确认是病毒）
- `QVM` = 360 的 **AI 机器学习引擎**（根据文件特征打分）
- `Malware.Gen` = "通用恶意软件家族"（特征归类）

我们的安装包被误判，主要有 3 个原因：

| 原因 | 说明 |
|---|---|
| ① **NSIS 安装器** | 安装程序会自动解压文件、写注册表、创建桌面快捷方式——这些是正常安装行为，但和"自解压木马"的行为很像 |
| ② **Go 语言编译** | 我们的软件用 Go 编写，Go 程序体积大、内部特征和不少恶意软件（如银狐木马）相似，是杀软误报的重灾区 |
| ③ **没有代码签名 + 新版本** | 安装包没有数字签名证书（个人开发者常见），每次发新版本文件特征都是新的，杀软云端没有"白名单"记录，首次见到就容易误判（360 和微软的 AI 引擎都会这样） |

**换句话说：越像"正经安装器"的文件，越容易被启发式引擎盯上。** 这是 360 QVM 引擎对"未签名的新安装包"的常见误报，并非我们软件有问题。

## 1.5 微软 Defender 也报了？（Trojan:Win32/Wacatac.B!ml）

如果你把安装包上传到 **VirusTotal** 检测，可能看到微软 Defender 报 `Trojan:Win32/Wacatac.B!ml`。

先看三个关键事实：

| 事实 | 说明 |
|---|---|
| **检测率 1 / 69** | 69 个杀毒引擎里只有微软 1 家报，其余 68 家（卡巴、ESET、Avast、Acronis 等）全部通过。真病毒不可能只有 1 家报 |
| **报的文件是 `$PLUGINSDIR/System.dll`** | 这是 **NSIS 安装器的官方标准组件**（NSIS 自带的 System 插件，用于写注册表/调用系统函数），**全世界所有用 NSIS 打包的安装程序都内置它**，不是我们的程序代码 |
| **`!ml` = 机器学习检测** | `Wacatac.B!ml` 的 `!ml` 后缀表示这是微软的 **AI 启发式引擎**在猜，不是病毒特征库确认。它对我们这种"未签名 + NSIS 安装器 + Go 程序"组合的误报，在微软官方社区有大量先例 |

> 验证：NSIS 官方发布的安装包里同样存在 `Plugins/*/System.dll`（我们本机对比过官方包，结构一致；本机打包用的是 NSIS v3.12）。
> 换句话说：**被报的是 NSIS 官方工具链的一部分，和我们写的软件代码没有关系**——换任何一家用 NSIS 打包的软件（包括很多商业软件）都会遇到同样情况。

微软误报申诉入口（免费，提交文件后一般 1-2 周处理）：
**https://www.microsoft.com/wdsi/filesubmission**

## 2. 怎么确认它没毒？（自证三步）

1. **多引擎扫描**：把安装包上传到 [VirusTotal](https://www.virustotal.com)（免费）——
   如果 69 个引擎里只有 1-2 家报（如 360 QVM、微软 ML），其他全部通过，就是实锤误报。v0.10.4 实测：**1 / 69**（仅微软 Defender ML 引擎报，且报的是 NSIS 标准组件 System.dll）。
2. **Windows Defender**：右键安装包 → 用 Windows 自带的 Microsoft Defender 扫描，通常结果 0 威胁。
3. **对哈希**：下载后核对文件 SHA256（下面的值），一致说明文件没被篡改：

```
SHA256: E21A5D144D29E6F4A445171C2D31C1B668A86BFBEE400EEA55260BA956D1CE44
```

> 校验方法（Windows）：在安装包所在目录按住 Shift 右键 →"在终端中打开"→ 输入
> `Get-FileHash .\roleplay-chat-amd64-installer.exe -Algorithm SHA256`，比对输出即可。

## 3. 被拦截了怎么办？

### 短期（1 分钟，先装上用）

打开 **360安全卫士** → 木马查杀 → **恢复区**（或"信任区"）→ 找到该文件 → **恢复并添加信任**。
也可以：右键任务栏 360 图标 → 暂时退出防护 → 安装完成后再开启。

### 中期（提交误报申诉，官方解除）

360 官方误报申诉入口：**https://fuwu.360.cn/shensu** →"软件风险提示申诉"（软件误报反馈）

提交材料：安装包文件 + 360 报毒截图 + 软件说明（个人开发的开源 AI 角色扮演应用）+ 你的邮箱。
官方处理时效：**1-2 个工作日**，处理完成后云端解除误报，之后大家下载都不再拦截。

### 长期（一劳永逸，我们这边会推进）

- 为安装包申请**代码签名证书**（OV 级，签名后信誉大幅提升）
- 注册 **360 软件开放平台**（open.soft.360.cn）提交软件检测，长期加入白名单
- 以后每次发布前把安装包上传 VirusTotal 预检，把检测报告放进发布说明

## 4. 我们的软件是什么？

**RoleplayChat** —— 一款开源的 Windows 桌面 AI 角色扮演应用（Go + Angular + SQLite）。

- 免浏览器、免配置，自带任意 OpenAI 兼容 API
- 兼容 SillyTavern 角色卡、世界书与预设
- 全部代码公开：https://github.com/cctv2333/RoleplayChat
- 下载页：https://github.com/cctv2333/RoleplayChat-release
- 数据只保存在你自己的电脑（`C:\ProgramData\RoleplayChat`），API Key 用 Windows DPAPI 加密存储

**开源即自证。** 任何声称"有木马"的说法，都可以用源码逐行验证——欢迎安全工程师来审计。

---

*如仍有疑问，请把 360 报毒截图 + VirusTotal 报告链接发给我们，我们会协助你向 360 提交申诉。*
