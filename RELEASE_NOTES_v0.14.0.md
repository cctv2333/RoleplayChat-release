# RoleplayChat v0.14.0 更新说明

> 发布日期：2026-09-24　|　上一安装包：2026-09-08
> 安装包：`roleplay-chat-amd64-installer.exe`（15.1 MB，15,165,504 字节）
> **SHA256**：`06A5A07EE4D9D7894ACD4F8CD373833F66245BB780EC8BC5F351E1AD54A30DAB`
> 数据与设置沿用原目录（`C:\ProgramData\RoleplayChat`），可直接覆盖安装。

本次是一个**功能大版本**，重点是「看图 / 找图 / 搜索 / 读懂网页」，并新增内置解析器 OCR。

---

## 一、识图：直接发图给 AI

- 工作模式下可**直接发送图片**给 AI 看（角色/群聊模式可在「设置」里开启）。
- 图片直接在**气泡里显示**，点击可**放大预览**。
- 放大预览提供两个保存方式：**「另存为…」**（自选目录与文件名）与**「存到工作目录」**。
- 历史消息中的旧图片会自动剥离，避免部分服务商（如 DeepSeek）因"历史带图"而报错。

## 二、让 AI「看见」自己找的图（新）

- AI 联网取到图片后，**支持视觉的模型会自动"看图"并自检是否与你的要求相符**；不符会换关键词重取。
- 只有支持视觉的模型才启用（其它模型不受影响）。

## 三、AI 联网取图 → 沙盘（新）

- 从网上取的图片**只进入"沙盘"**（可丢弃的临时空间，默认 7 天自动清理），**不会进入你的工作目录**。
- 支持直接给关键词找图（如"找一张二次元美少女的图片"）。
- 「设置」里可查看沙盘占用、一键清空；想长期保留需在放大预览里**显式另存**。

## 四、搜索与读网页：大改造

- **免 Key 搜索升级**：改用 Bing RSS 为主（结果更干净），失败自动降级到多来源合并；自动去重、剔除广告与跳转壳。
- **能读到正文了**：搜索后会**自动读取前几条结果的正文**；遇到 JS 渲染页，用**本机 Edge 无头渲染**兜底。
- **新增搜索提供方 Querit**（设置 → 联网提供方，需自备 Key，免费 1000 次/月）：搜索的同时返回网页正文。
- 修复「联网开关」关不掉个别抓页工具的问题。

## 五、内置解析器 OCR（新）

- **扫描版 PDF** 会自动渲染成图片、再交给视觉模型识别文字——**不需要安装任何额外组件**。
- 知识库导入扫描件后可被正常检索到（此前扫描件只能收到水印等无效内容）。

## 六、模型能力检测

- 模型下拉框会显示**「视觉 / 推理 / 工具」徽章**，一眼看出当前模型能不能看图、能不能调工具。

## 七、知识库（K1–K5）

- 关键词 + 向量混合检索（RRF 融合）、自动注入到对话。
- 知识库界面改为可展开的**文件夹树**，含导入报告与分页。

## 八、体验修复

- **输入草稿按会话保存**：切换会话/切换角色后，之前打了一半的内容不会丢（重启也保留）。
- **复制更自由**：消息正文可像普通文档一样**选中一段复制**，不再只能整条复制。
- 工具参数报错更明确：区分"参数解析失败"与"缺少参数"，并回显收到的内容，便于排查。

---

## 升级与兼容

- 直接运行新安装包覆盖安装即可；数据（会话/角色/世界书/知识库/设置）保留。
- 沙盘是新增功能，首次使用会自动创建目录。
- 如需使用 Querit/博查/Tavily 搜索或视觉相关能力，请在「设置」中按需填写相应 Key。

---

*本说明随 v0.14.0 安装包发布。*
